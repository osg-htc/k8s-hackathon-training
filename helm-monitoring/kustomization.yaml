apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: podinfo
resources:
  #- namespace.yaml
  - helmhttpsrepository.yaml
  - helmrelease.yaml
    #configMapGenerator:
    #- name: podinfo-values
    #files:
    #  - values.yaml=my-values.yaml
    #configurations:
    #- kustomizeconfig.yaml
